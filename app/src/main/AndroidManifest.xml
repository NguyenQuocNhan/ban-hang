<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xyz">
    <!-- CardShopActivity get location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Xyz">
        <activity android:name=".Message0Activity"></activity>
        <activity android:name=".UpdateManagerProductActivity" />
        <activity android:name=".ManagerProductActivity" />
        <activity android:name=".ManagerOrderActivity" />
        <activity android:name=".DetailOrderActivity" />
        <activity android:name=".CreateProductActivity" />
        <activity android:name=".UpdateUserActivity" />
        <activity android:name=".CardShopActivity" />
        <activity android:name=".MessageActivity" />
        <activity android:name=".SeeImageActivity" />
        <activity android:name=".ProductActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>